import { BrandLoader } from "@/components/ui/Loaders";

export default function Loading() {
  return <BrandLoader title="Loading auth screens" subtitle="Preparing your login and registration experience." />;
}
