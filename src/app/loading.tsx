import { BrandLoader } from "@/components/ui/Loaders";

export default function Loading() {
  return <BrandLoader title="Loading Flowboard" subtitle="One moment while we prepare your workspace." />;
}
