import { BrandLoader } from "@/components/ui/Loaders";

export default function Loading() {
  return <BrandLoader title="Loading dashboard" subtitle="Preparing your projects, tasks, and analytics." />;
}
